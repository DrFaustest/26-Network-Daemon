This is a python program that pings the network, gets the IP address and MAC address of the devices connected to the network and stores it in a database then searches for the device manufacturer using the MAC address and stores it in the database.